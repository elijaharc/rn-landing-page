import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    imageContainer: {
        flexDirection: 'row',
        marginTop: 25,
        marginBottom: 35
    },
    imageStyle: {
        borderRadius: 50,
        overflow: 'hidden',
        margin: -3
    }
});

export default styles;